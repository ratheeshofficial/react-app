import  React from 'react'

export default function MovieList(){
    const movies = ['KGF - 2']
    return (
        <div>
            {/* <h1>MovieList</h1>
            {movies.map((movie, key) => {
                return <p key={`movie-list-${key}`}>{movie}</p>
            })} */}
        </div>
    )
}